apiVersion: v1
kind: Service
metadata:
  name: service-order-billing-service
  namespace: service-order
  labels:
    app: service-order-billing-service
spec:
  type: ClusterIP
  selector:
    app: service-order-billing-service
  ports:
    - name: http
      port: 80
      targetPort: 8000

